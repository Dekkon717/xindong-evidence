# `she-love-me` 原理拆解

## 一句话

它不是“训练了一个恋爱模型”，而是把聊天记录加工成一份供 Agent 阅读的证据包：一份覆盖全量的数字统计，加上一份覆盖关键阶段的分层对话样本，再由模型按固定框架写出报告。

## 数据管线

```text
微信 / QQ / JSON / Markdown
          ↓
     格式转换与时间归一化
          ↓
     messages.json（统一消息契约）
          ├── stats_analyzer.py
          │      ↓
          │   stats.json（全量统计）
          └── build_chat_history.py
                 ↓
              chat_history.txt（关键窗口采样）
          ↓
        Agent 按证据框架分析
          ↓
  analysis.json → generate_html_report.py → HTML 报告
```

## 统计层做了什么

`stats_analyzer.py` 是确定性的规则引擎，主要包含：

1. 按 3 小时空档切分对话，并统计每次是谁先开口。
2. 对相邻的跨发送者消息计算回复时间，忽略 10 秒以内和超过 24 小时的间隔。
3. 统计连续 3 条以上由同一方发送的消息，作为“轰炸”信号。
4. 用冷淡词表和长度不超过 2 的文本识别短回复。
5. 统计晚安/早安、活跃小时、每日消息趋势、消息长度、撤回消息。
6. 用代词、模糊词、条件句、正负情绪词做一个很轻量的语言学侧写。

主动指数、被爱指数、冷淡指数都不是模型生成的，而是用消息占比、主动发起占比、回复速度比、连续发送、晚安发起、冷淡占比等字段加权计算到 0–100。这个设计让数字有来源，但词表和阈值仍然是启发式规则，不能理解成心理学量表。

## 采样层为什么存在

完整聊天记录可能太大，不能原样交给模型。因此 `build_chat_history.py` 先给出最近 1 个月、3 个月、半年、1 年和全量选项，再生成四类窗口：

- 关系起源：最早 100 条文字消息
- 高冲突区间：负向词密度最高的连续 100 条
- 最近 30 天：最多 200 条文字消息
- 修复时刻：每次超过 24 小时沉默后的后续消息

这相当于把上下文预算花在“关系怎样开始、哪里坏掉、现在怎样、坏掉后谁修复”四个问题上。它解决的是模型上下文和叙事代表性，不是统计精度；所以项目明确要求模型同时读取全量 `stats.json`。

## Agent 层的约束

Skill 文件把分析顺序固定为人物画像、关系动态、危险预警、军师建议等模块，并提出几条重要护栏：没有时间戳原话就不诊断；危险信号需要量化条件和文本条件同时满足；证据不足的字段留空；先描述对话现场，再引入理论名词。这些约束主要用于降低模型把偶然一句话过度解释成心理结论的风险。

## 报告层

`generate_html_report.py` 读取 `stats.json` 与模型生成的 `analysis.json`，将结构化字段渲染为暗色 HTML 报告。也就是说，HTML 本身不是分析引擎，而是最后的展示层。

## 我们这个版本的取舍

本目录实现的是一个可直接打开的浏览器原型，保留了最值得复核的部分：导入、归一化、互动指标、趋势、证据锚点和建议，并增加了关系阶段、Sternberg 三角、Gottman 风格信号、语言模式、双方沟通画像、依恋倾向、情感可得性、风险雷达、修复和五个发现。暂不复刻微信本机导出/解密；依恋倾向、情感可得性和风险雷达只作可观察信号展示，不伪装成确定诊断。下一步若接入模型，建议让模型只读取本地统计结果和用户明确选择的对话窗口，并为每个结论强制保留原话与时间戳。
